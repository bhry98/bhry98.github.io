{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"\ud83d\udc4b Hi, I'm BHR Abdelrahman","text":"<p>Welcome to my personal documentation and portfolio.</p> <p>I'm a Software &amp; Infrastructure Engineer passionate about building scalable applications, cloud infrastructure, automation, and open-source Laravel packages.</p>"},{"location":"#open-source","title":"\u2764\ufe0f Open Source","text":"<p>I enjoy building tools that help developers write better software faster.</p> <p>Contributions, feedback, and feature requests are always welcome.</p>"},{"location":"#contact","title":"\ud83d\udcec Contact","text":"<p>Feel free to reach out.</p> <ul> <li>GitHub: bhry98</li> <li>LinkedIn: (Abdelrahman Mahmoud)</li> </ul>"},{"location":"#support","title":"\u2b50 Support","text":"<p>If you find one of my projects useful:</p> <ul> <li>\u2b50 Star the repository</li> <li>\ud83d\udc1b Report issues</li> <li>\ud83d\udca1 Suggest new features</li> <li>\ud83e\udd1d Contribute via pull requests</li> </ul> <p>\"Build once. Improve forever.\"</p>"},{"location":"packages/","title":"BHRY98 Packages","text":""},{"location":"packages/#commands","title":"Commands","text":"<ul> <li><code>mkdocs new [dir-name]</code> - Create a new project.</li> <li><code>mkdocs serve</code> - Start the live-reloading docs server.</li> <li><code>mkdocs build</code> - Build the documentation site.</li> <li><code>mkdocs -h</code> - Print help message and exit.</li> </ul>"},{"location":"packages/laravel-api-response/","title":"Laravel API Response","text":"<p>A fluent, extensible, highly configurable API response toolkit for Laravel 11, 12 and 13. Zero configuration to get started, endlessly customizable when you need it.</p> <pre><code>return api()-&gt;created($user);\n\nreturn api()\n    -&gt;success()\n    -&gt;message('User created.')\n    -&gt;data($user)\n    -&gt;meta(['foo' =&gt; 'bar'])\n    -&gt;headers(['X-Foo' =&gt; 'bar'])\n    -&gt;send();\n</code></pre>"},{"location":"packages/laravel-api-response/#why-this-package","title":"Why this package","text":"<ul> <li>One fluent builder (<code>api()</code> / <code>ApiResponse</code>) for every response your API sends - success, error, validation,   pagination, resources.</li> <li>Configurable envelope - rename keys, wrap the payload, switch between <code>default</code>/<code>minimal</code>/<code>mobile</code>/<code>jsonapi</code>   presets, or write your own formatter.</li> <li>Automatic data detection - pass an Eloquent model, a <code>Collection</code>, a <code>JsonResource</code>, a paginator, or a plain   array/DTO; it's normalized the same way every time.</li> <li>Cross-cutting concerns as opt-in middleware - rate limiting, request/response audit logging, and   Accept-Language-aware locale resolution, all driven by config.</li> <li>Hooks, events, macros and presets for every extension point, so nothing requires forking the package.</li> <li>Fully backward compatible with the pre-2.0 <code>BResponseSuccess</code>/<code>BResponseError</code>/... API.</li> </ul>"},{"location":"packages/laravel-api-response/#where-to-go-next","title":"Where to go next","text":"Page What's in it Installation Composer install, publishing config/lang/migrations, requirements. Responses Building and sending responses: basic usage, ready-made shortcuts, templates, macros, automatic data detection, pagination, resources. Customization Presets &amp; envelope shape, global metadata, debug mode, versioning, localization, rate limiting, audit logging, hooks, events, exception handling, testing helpers, extending the package. Upgrade Guide Backward compatibility with the pre-2.0 API and the package's internal architecture."},{"location":"packages/laravel-api-response/#quick-example","title":"Quick example","text":"<pre><code>use Bhry98\\LaravelApiResponse\\Facades\\ApiResponse;\n\nreturn api()\n    -&gt;success()\n    -&gt;message('User created.')\n    -&gt;data($user)\n    -&gt;send();\n\n// identical, via the facade\nreturn ApiResponse::success()-&gt;message('User created.')-&gt;data($user)-&gt;send();\n</code></pre> <p>Default JSON shape:</p> <pre><code>{\n    \"status\": true,\n    \"message\": \"User created.\",\n    \"data\": {\n        \"id\": 1\n    }\n}\n</code></pre>"},{"location":"packages/laravel-api-response/customization/","title":"Customization","text":""},{"location":"packages/laravel-api-response/customization/#presets-configurable-json-structure","title":"Presets &amp; configurable JSON structure","text":"<p>Every top-level key is renameable:</p> <pre><code>'keys' =&gt; [\n    'status' =&gt; 'success',\n    'message' =&gt; 'msg',\n    'data' =&gt; 'result',\n],\n</code></pre> <pre><code>{ \"success\": true, \"msg\": \"...\", \"result\": {} }\n</code></pre> <p>Set <code>'wrap' =&gt; true</code> to nest the whole envelope under a top-level <code>data</code> key.</p> <p>Four built-in presets control the overall response shape (not just key names):</p> Preset Behaviour <code>default</code> The standard <code>{status, message, data, errors?, meta?, links?}</code> envelope. <code>minimal</code> Only <code>{status, message, data, errors?}</code> - no meta/links/extras/debug. <code>mobile</code> Same as <code>default</code> but recursively strips <code>null</code> values to save bandwidth. <code>jsonapi</code> A JSON:API-inspired <code>{data, meta, links, errors}</code> shape (not a strict spec implementation). <pre><code>config(['bhry98-api-response.preset' =&gt; 'minimal']); // global default\n\nreturn api()-&gt;preset('jsonapi')-&gt;success()-&gt;data($user); // per response\n</code></pre> <p>Register your own preset:</p> <pre><code>use Bhry98\\LaravelApiResponse\\Contracts\\{PresetContract, ResponseFormatterContract};\nuse Bhry98\\LaravelApiResponse\\Presets\\PresetManager;\n\nclass ShoutyFormatter implements ResponseFormatterContract\n{\n    public function format(\\Bhry98\\LaravelApiResponse\\Data\\ResponsePayload $payload): array\n    {\n        return ['STATUS' =&gt; $payload-&gt;success, 'MESSAGE' =&gt; strtoupper($payload-&gt;message)];\n    }\n}\n\nclass ShoutyPreset implements PresetContract\n{\n    public function __construct(private ShoutyFormatter $formatter) {}\n    public function formatter(): ResponseFormatterContract { return $this-&gt;formatter; }\n}\n\n// in a service provider\napp(PresetManager::class)-&gt;register('shouty', ShoutyPreset::class);\n\nreturn api()-&gt;preset('shouty')-&gt;success()-&gt;message('done');\n</code></pre>"},{"location":"packages/laravel-api-response/customization/#global-metadata","title":"Global metadata","text":"<p>Opt in per key:</p> <pre><code>'meta' =&gt; [\n    'enabled' =&gt; ['request_id', 'execution_time', 'api_version', 'locale'],\n],\n</code></pre> <pre><code>{ \"meta\": { \"request_id\": \"...\", \"execution_time\": \"12.4ms\", \"api_version\": \"v1\", \"locale\": \"en\" } }\n</code></pre> <p>Explicit <code>-&gt;meta([...])</code> values always win over the automatic ones on key collisions. Add your own provider by implementing <code>MetaProviderContract</code> and registering it under <code>meta.providers</code>.</p> <p>There is also an optional <code>AttachRequestId</code> middleware that reuses an incoming <code>X-Request-Id</code> header (or generates one) and echoes it back as a response header, so the <code>request_id</code> meta stays consistent with your logs:</p> <pre><code>// bootstrap/app.php\n-&gt;withMiddleware(function (Middleware $middleware) {\n    $middleware-&gt;append(\\Bhry98\\LaravelApiResponse\\Middleware\\AttachRequestId::class);\n})\n</code></pre>"},{"location":"packages/laravel-api-response/customization/#debug-mode","title":"Debug mode","text":"<p>Development only - requires both flags to be true, so it can never leak into production by accident:</p> <pre><code>'debug' =&gt; [\n    'enabled' =&gt; env('API_RESPONSE_DEBUG', false),\n    'include' =&gt; ['queries', 'time', 'memory', 'exception', 'trace'],\n],\n</code></pre> <pre><code>{ \"debug\": { \"execution_time_ms\": 12.4, \"memory_usage_mb\": 8.1, \"queries\": [...] } }\n</code></pre>"},{"location":"packages/laravel-api-response/customization/#api-versioning","title":"API versioning","text":"<pre><code>'version' =&gt; env('API_RESPONSE_VERSION', 'v1'),\n</code></pre> <pre><code>return api()-&gt;version('v2')-&gt;success(); // overrides for this response\n</code></pre> <p>Exposed automatically via the <code>api_version</code> meta provider when enabled.</p>"},{"location":"packages/laravel-api-response/customization/#localization","title":"Localization","text":""},{"location":"packages/laravel-api-response/customization/#translating-messages","title":"Translating messages","text":"<pre><code>return api()-&gt;updated()-&gt;localize('bhry98-api-response::api.updated');\n</code></pre> <p>The package ships <code>lang/en/api.php</code> (publishable to <code>lang/vendor/bhry98-api-response</code>) with an entry per ready-made shortcut. <code>config('bhry98-api-response.messages')</code> remains the primary/default source so a config override is never silently shadowed by a translation - use <code>localize()</code> explicitly when you want locale-driven messages instead.</p>"},{"location":"packages/laravel-api-response/customization/#resolving-the-requests-locale-accept-language-user-column","title":"Resolving the request's locale (Accept-Language + user column)","text":"<p>Opt-in per-request locale resolution via the <code>api.locale</code> middleware alias. It resolves a locale in this priority order and calls <code>App::setLocale()</code>:</p> <ol> <li>The <code>Accept-Language</code> request header (first tag, e.g. <code>fr</code> out of <code>fr-CA;q=0.9,en;q=0.5</code>), matched against <code>locale.supported</code> when that list is non-empty.</li> <li>The authenticated user's locale column - the column name is configurable, so this works whatever your <code>users</code> table calls it (<code>locale</code>, <code>lang</code>, <code>preferred_language</code>, ...).</li> <li><code>locale.fallback</code> (defaults to <code>config('app.locale')</code>).</li> </ol> <pre><code>'locale' =&gt; [\n    'enabled' =&gt; env('API_RESPONSE_LOCALE_ENABLED', false),\n    'supported' =&gt; ['en', 'ar', 'fr'], // empty array = accept any tag from the header\n    'user_column' =&gt; env('API_RESPONSE_LOCALE_USER_COLUMN', 'locale'),\n    'fallback' =&gt; env('API_RESPONSE_LOCALE_FALLBACK', config('app.locale', 'en')),\n],\n</code></pre> <pre><code>// bootstrap/app.php\n-&gt;withMiddleware(function (Middleware $middleware) {\n    $middleware-&gt;appendToGroup('api', 'api.locale');\n})\n</code></pre> <p>Combine with the <code>locale</code> global metadata provider (above) to echo the resolved locale back on every response.</p>"},{"location":"packages/laravel-api-response/customization/#rate-limiting","title":"Rate limiting","text":"<p>Opt-in throttling for any route/group via the <code>api.rate_limit</code> middleware alias. It wraps Laravel's own <code>RateLimiter</code>, but returns a standard <code>api()-&gt;tooManyRequests()</code> envelope - with <code>Retry-After</code>/<code>X-RateLimit-*</code> headers - instead of the framework's default <code>ThrottleRequestsException</code> response.</p> <pre><code>'rate_limit' =&gt; [\n    'enabled' =&gt; env('API_RESPONSE_RATE_LIMIT_ENABLED', false),\n    'max_attempts' =&gt; env('API_RESPONSE_RATE_LIMIT_MAX_ATTEMPTS', 60),\n    'decay_seconds' =&gt; env('API_RESPONSE_RATE_LIMIT_DECAY_SECONDS', 60),\n    'by' =&gt; env('API_RESPONSE_RATE_LIMIT_BY', 'ip'), // \"ip\", \"user\" or \"route\"\n    'key_prefix' =&gt; env('API_RESPONSE_RATE_LIMIT_PREFIX', 'api-response-rate-limit'),\n    'message' =&gt; null, // null falls back to messages.too_many_requests\n],\n</code></pre> <pre><code>// bootstrap/app.php\n-&gt;withMiddleware(function (Middleware $middleware) {\n    $middleware-&gt;appendToGroup('api', 'api.rate_limit');\n})\n</code></pre> <p>Override the configured <code>max_attempts</code>/<code>decay_seconds</code> for a specific route via middleware parameters:</p> <pre><code>Route::post('/login', ...)-&gt;middleware('api.rate_limit:5,1'); // 5 attempts per minute\n</code></pre> <p><code>by: 'user'</code> buckets authenticated requests by the user's auth identifier and falls back to IP for guests; <code>by: 'route'</code> buckets by route name (or URI path when unnamed).</p>"},{"location":"packages/laravel-api-response/customization/#audit-logging","title":"Audit logging","text":"<p>Opt-in request/response auditing via the <code>api.audit</code> middleware alias. Every matching request is written to the <code>api_response_audit_logs</code> table (name configurable) through the queued <code>StoreAuditLogEntry</code> job by default, so logging never adds latency to the actual response - set <code>queue.enabled</code> to <code>false</code> to write synchronously instead (handy for local dev/testing).</p> <pre><code>'audit_log' =&gt; [\n    'enabled' =&gt; env('API_RESPONSE_AUDIT_LOG_ENABLED', false),\n\n    'connection' =&gt; env('API_RESPONSE_AUDIT_LOG_DB_CONNECTION'), // null = default DB connection\n    'table' =&gt; env('API_RESPONSE_AUDIT_LOG_TABLE', 'api_response_audit_logs'),\n\n    'queue' =&gt; [\n        'enabled' =&gt; env('API_RESPONSE_AUDIT_LOG_QUEUE_ENABLED', true),\n        'connection' =&gt; env('API_RESPONSE_AUDIT_LOG_QUEUE_CONNECTION'), // null = config('queue.default')\n        'queue' =&gt; env('API_RESPONSE_AUDIT_LOG_QUEUE_NAME'), // null = the connection's default queue\n    ],\n\n    'capture' =&gt; [\n        'request_body' =&gt; true,\n        'response_body' =&gt; true,\n        'headers' =&gt; false,\n    ],\n\n    'methods' =&gt; [], // e.g. ['POST', 'PUT', 'PATCH', 'DELETE'] - empty means every method\n    'exclude_routes' =&gt; [], // route names or URI patterns (fnmatch), e.g. 'telescope*'\n\n    'hidden' =&gt; [\n        'password', 'password_confirmation', 'token', 'secret',\n        'authorization', 'api_key', 'access_token', 'refresh_token', 'card_number', 'cvv',\n    ],\n],\n</code></pre> <pre><code>php artisan vendor:publish --tag=bhry98-api-response-migrations\nphp artisan migrate\n</code></pre> <pre><code>// bootstrap/app.php\n-&gt;withMiddleware(function (Middleware $middleware) {\n    $middleware-&gt;appendToGroup('api', 'api.audit');\n})\n</code></pre> <ul> <li><code>queue.connection</code>/<code>queue.queue</code> map directly onto <code>StoreAuditLogEntry::onConnection()</code>/<code>onQueue()</code>. Leave them <code>null</code> to use your app's default queue connection (<code>config('queue.default')</code>, e.g. <code>sync</code>, <code>database</code>, <code>redis</code>, ...) - set <code>queue.connection</code> to <code>'database'</code> (or <code>'redis'</code>, etc.) explicitly if you want audit logs on a different connection than the rest of your app's jobs.</li> <li>Anything listed under <code>hidden</code> is redacted (replaced with <code>\"***\"</code>) wherever it appears as a request/response body (or header, if <code>capture.headers</code> is on) key, at any nesting depth, before the entry is persisted.</li> <li>Query the log like any other Eloquent model:</li> </ul> <pre><code>use Bhry98\\LaravelApiResponse\\Models\\AuditLog;\n\nAuditLog::where('user_id', $user-&gt;id)-&gt;latest('created_at')-&gt;get();\n</code></pre>"},{"location":"packages/laravel-api-response/customization/#hooks","title":"Hooks","text":"<p>Run arbitrary logic against every response before it is formatted:</p> <pre><code>use Bhry98\\LaravelApiResponse\\Facades\\ApiResponse;\nuse Bhry98\\LaravelApiResponse\\Data\\ResponsePayload;\n\nApiResponse::before(function (ResponsePayload $payload) {\n    $payload-&gt;extra['server'] = gethostname();\n});\n\nApiResponse::after(function (ResponsePayload $payload) {\n    Log::info('api.response', ['status' =&gt; $payload-&gt;success]);\n});\n</code></pre>"},{"location":"packages/laravel-api-response/customization/#events","title":"Events","text":"<pre><code>Bhry98\\LaravelApiResponse\\Events\\ResponseCreated::class; // fired for every response\nBhry98\\LaravelApiResponse\\Events\\ResponseFailed::class;  // fired when success === false\nBhry98\\LaravelApiResponse\\Events\\ResponseSent::class;    // fired once the JsonResponse is built\n</code></pre>"},{"location":"packages/laravel-api-response/customization/#exception-handling","title":"Exception handling","text":"<p>Fully opt-in - off by default:</p> <pre><code>'exceptions' =&gt; [\n    'override_exception_handler' =&gt; env('API_RESPONSE_OVERRIDE_EXCEPTIONS', false),\n],\n</code></pre> <p>When enabled, <code>ValidationException</code>, <code>AuthenticationException</code>, <code>AuthorizationException</code>, <code>ModelNotFoundException</code> and any <code>HttpExceptionInterface</code> are automatically formatted as <code>api()</code> responses for JSON/<code>api/*</code> requests; anything else becomes <code>api()-&gt;internal()</code> (with the real message only when <code>app.debug</code> is true).</p>"},{"location":"packages/laravel-api-response/customization/#overriding-the-default-response-json","title":"Overriding the default <code>response()-&gt;json()</code>","text":"<p>Also fully opt-in:</p> <pre><code>'override_default_response' =&gt; env('API_RESPONSE_OVERRIDE_DEFAULT', false),\n</code></pre> <pre><code>// with the flag enabled\nreturn response()-&gt;json(['id' =&gt; 1]);\n// =&gt; {\"status\": true, \"data\": {\"id\": 1}}\n</code></pre> <p>Never forced - leave it <code>false</code> (the default) and <code>response()-&gt;json()</code> behaves exactly like stock Laravel.</p>"},{"location":"packages/laravel-api-response/customization/#testing-helpers","title":"Testing helpers","text":"<p>Registered automatically on <code>Illuminate\\Testing\\TestResponse</code> - no trait/import needed:</p> <pre><code>$response-&gt;assertApiSuccess('User created.');\n$response-&gt;assertApiError('Something went wrong.');\n$response-&gt;assertApiValidation(['email']);\n$response-&gt;assertApiMessage('User created.');\n$response-&gt;assertApiStatus(true);\n$response-&gt;assertApiData(['id' =&gt; 1]);\n</code></pre>"},{"location":"packages/laravel-api-response/customization/#extending-the-package","title":"Extending the package","text":"<ul> <li>Formatters - implement <code>ResponseFormatterContract</code> for a fully custom envelope shape.</li> <li>Presets - implement <code>PresetContract</code> and register via <code>PresetManager::register()</code>.</li> <li>Data resolvers - the default <code>DataResolverContract</code> binding (<code>DataResolver</code>) can be swapped in your own service provider if you need custom auto-detection.</li> <li>Meta providers - implement <code>MetaProviderContract</code>, register under <code>meta.providers</code>, then list the key under <code>meta.enabled</code>.</li> <li>Macros - <code>ResponseBuilder::macro()</code> / <code>ApiResponse::macro()</code> for one-off custom shortcuts.</li> <li>Hooks - <code>ApiResponse::before()</code> / <code>ApiResponse::after()</code> for cross-cutting concerns (logging, request ids, ...).</li> <li>Events - listen to <code>ResponseCreated</code> / <code>ResponseFailed</code> / <code>ResponseSent</code>.</li> </ul>"},{"location":"packages/laravel-api-response/customization/#next","title":"Next","text":"<p>See the Upgrade Guide for backward compatibility with the pre-2.0 API and a look at the package's internal architecture.</p>"},{"location":"packages/laravel-api-response/installation/","title":"Installation","text":""},{"location":"packages/laravel-api-response/installation/#requirements","title":"Requirements","text":"PHP ^8.2 Laravel 11.x, 12.x or 13.x"},{"location":"packages/laravel-api-response/installation/#install-via-composer","title":"Install via Composer","text":"<pre><code>composer require bhry98/laravel-api-response\n</code></pre> <p>The service provider and the <code>ApiResponse</code> facade alias are auto-discovered - nothing else is required. Every feature documented here works out of the box with sensible defaults; rate limiting, audit logging and locale resolution are all opt-in.</p>"},{"location":"packages/laravel-api-response/installation/#publishing","title":"Publishing","text":""},{"location":"packages/laravel-api-response/installation/#config","title":"Config","text":"<pre><code>php artisan vendor:publish --tag=bhry98-api-response-config\n</code></pre> <p>Creates <code>config/bhry98-api-response.php</code> with commented sections for keys, wrapping, messages, templates, meta, versioning, debug mode, exception handling, the response-factory override, rate limiting, audit logging and locale resolution. Every feature in these docs maps to a section in that file.</p>"},{"location":"packages/laravel-api-response/installation/#translations","title":"Translations","text":"<pre><code>php artisan vendor:publish --tag=bhry98-api-response-lang\n</code></pre> <p>Publishes <code>lang/vendor/bhry98-api-response</code> - see Localization.</p>"},{"location":"packages/laravel-api-response/installation/#audit-log-migration","title":"Audit log migration","text":"<p>Only needed if you turn on audit logging:</p> <pre><code>php artisan vendor:publish --tag=bhry98-api-response-migrations\nphp artisan migrate\n</code></pre> <p>This publishes a timestamped migration that creates the <code>api_response_audit_logs</code> table (the table name itself is configurable via <code>audit_log.table</code>).</p>"},{"location":"packages/laravel-api-response/installation/#next","title":"Next","text":"<p>Head to Responses to start sending your first <code>api()</code> response, or to Customization to wire up rate limiting, audit logging or locale resolution.</p>"},{"location":"packages/laravel-api-response/responses/","title":"Responses","text":""},{"location":"packages/laravel-api-response/responses/#basic-usage","title":"Basic usage","text":"<p>The <code>api()</code> helper (and the <code>ApiResponse</code> facade) resolve a fresh fluent builder every time you call them - safe to chain, never shared state between responses.</p> <pre><code>use Bhry98\\LaravelApiResponse\\Facades\\ApiResponse;\n\nreturn api()\n    -&gt;success()\n    -&gt;message('User created.')\n    -&gt;data($user)\n    -&gt;send();\n\n// identical, via the facade\nreturn ApiResponse::success()-&gt;message('User created.')-&gt;data($user)-&gt;send();\n</code></pre> <p><code>ResponseBuilder</code> implements Laravel's <code>Responsable</code> contract, so you can skip <code>-&gt;send()</code> entirely and return the builder directly from a controller or route closure:</p> <pre><code>return api()-&gt;created($user);\n</code></pre> <p>Default JSON shape:</p> <pre><code>{\n  \"status\": true,\n  \"message\": \"User created.\",\n  \"data\": { \"id\": 1 }\n}\n</code></pre>"},{"location":"packages/laravel-api-response/responses/#ready-made-responses","title":"Ready-made responses","text":""},{"location":"packages/laravel-api-response/responses/#http-shortcuts","title":"HTTP shortcuts","text":"<pre><code>api()-&gt;ok($data);              // 200\napi()-&gt;created($user);         // 201\napi()-&gt;accepted();             // 202\napi()-&gt;noContent();            // 204, empty body\napi()-&gt;badRequest($msg, $errors); // 400\napi()-&gt;unauthorized();         // 401\napi()-&gt;forbidden();            // 403\napi()-&gt;notFound();             // 404\napi()-&gt;conflict();             // 409\napi()-&gt;unprocessable($errors); // 422\napi()-&gt;tooManyRequests();      // 429\napi()-&gt;internal();             // 500\napi()-&gt;serviceUnavailable();   // 503\n</code></pre>"},{"location":"packages/laravel-api-response/responses/#business-shortcuts","title":"Business shortcuts","text":"<pre><code>api()-&gt;saved($model);\napi()-&gt;updated($model);\napi()-&gt;deleted();\napi()-&gt;restored($model);\napi()-&gt;archived($model);\napi()-&gt;verified($user);\napi()-&gt;registered($user);      // 201\napi()-&gt;loggedIn($user);\napi()-&gt;loggedOut();\napi()-&gt;passwordChanged();\napi()-&gt;uploaded($file);        // 201\napi()-&gt;download($url);\napi()-&gt;emailSent();\napi()-&gt;otpSent();\n</code></pre> <p>Every shortcut accepts an optional message override (<code>api()-&gt;updated('Custom message.')</code>) and pulls its default from <code>config('bhry98-api-response.messages')</code> otherwise.</p>"},{"location":"packages/laravel-api-response/responses/#advanced-usage","title":"Advanced usage","text":"<pre><code>return api()\n    -&gt;success()\n    -&gt;message('Order placed.')\n    -&gt;data($order)\n    -&gt;meta(['queue_position' =&gt; 4])\n    -&gt;links(['tracking' =&gt; route('orders.track', $order)])\n    -&gt;extra(['request_id' =&gt; (string) Str::uuid()])\n    -&gt;headers(['X-Order-Id' =&gt; $order-&gt;id])\n    -&gt;cookie(Cookie::create('last_order')-&gt;withValue($order-&gt;id))\n    -&gt;version('v2')\n    -&gt;send();\n</code></pre> <ul> <li><code>data()</code> / <code>from()</code> - attach a payload (auto-detected, see below).</li> <li><code>meta()</code> / <code>links()</code> - merge into the <code>meta</code> / <code>links</code> blocks.</li> <li><code>errors()</code> - merge into the <code>errors</code> block (used automatically by <code>badRequest()</code>/<code>unprocessable()</code>).</li> <li><code>extra()</code> - merge extra keys into the top level of the envelope.</li> <li><code>headers()</code> / <code>cookie()</code> / <code>cookies()</code> - attach to the final <code>JsonResponse</code>.</li> <li><code>statusCode()</code> - set any arbitrary HTTP status.</li> <li><code>preset()</code> - pick a response shape for this call only (see Presets).</li> <li><code>version()</code> - override <code>config('bhry98-api-response.version')</code> for this response.</li> <li><code>localize($key)</code> - translate the current (or given) message through Laravel's translator.</li> </ul>"},{"location":"packages/laravel-api-response/responses/#templates","title":"Templates","text":"<p>Define reusable, named responses in config:</p> <pre><code>// config/bhry98-api-response.php\n'responses' =&gt; [\n    'user_created' =&gt; [\n        'status' =&gt; true,\n        'message' =&gt; 'User created successfully.',\n        'code' =&gt; 201,\n    ],\n    'invoice_paid' =&gt; [\n        'status' =&gt; true,\n        'message' =&gt; 'Invoice paid.',\n        'code' =&gt; 200,\n    ],\n],\n</code></pre> <pre><code>return api()-&gt;template('user_created');\n\n// override individual fields per call\nreturn api()-&gt;template('user_created', ['data' =&gt; $user]);\n</code></pre>"},{"location":"packages/laravel-api-response/responses/#macros","title":"Macros","text":"<p>Register your own reusable responses - <code>ResponseBuilder</code> (and therefore <code>api()</code>/<code>ApiResponse</code>) is fully <code>Macroable</code>:</p> <pre><code>use Bhry98\\LaravelApiResponse\\Facades\\ApiResponse;\n\nApiResponse::macro('invoicePaid', function (Invoice $invoice) {\n    /** @var \\Bhry98\\LaravelApiResponse\\Builder\\ResponseBuilder $this */\n    return $this-&gt;success()-&gt;message('Invoice paid.')-&gt;data($invoice);\n});\n\nreturn api()-&gt;invoicePaid($invoice);\n</code></pre>"},{"location":"packages/laravel-api-response/responses/#automatic-data-detection","title":"Automatic data detection","text":"<p><code>data()</code> (and its aliases <code>from()</code>, <code>resource()</code>, <code>resourceCollection()</code>, <code>paginate()</code>) automatically normalize whatever you pass in:</p> <pre><code>return api()-&gt;from($user);              // Eloquent model\nreturn api()-&gt;from($users);              // Collection\nreturn api()-&gt;from(UserResource::make($user));         // JsonResource\nreturn api()-&gt;from(UserResource::collection($users));  // ResourceCollection\nreturn api()-&gt;from($paginator);          // LengthAwarePaginator / CursorPaginator\nreturn api()-&gt;from($invoiceDto);         // any Arrayable DTO\nreturn api()-&gt;from(['id' =&gt; 1]);         // plain array - passed through untouched\n</code></pre> <p>Pagination and resource-collection meta/links are lifted into the envelope's top-level <code>meta</code>/<code>links</code> automatically.</p>"},{"location":"packages/laravel-api-response/responses/#pagination","title":"Pagination","text":"<pre><code>return api()-&gt;paginate($users);\n</code></pre> <pre><code>{\n  \"status\": true,\n  \"message\": \"Operation successful.\",\n  \"data\": [ ... ],\n  \"meta\": { \"current_page\": 1, \"last_page\": 5, \"per_page\": 15, \"total\": 72, \"from\": 1, \"to\": 15 },\n  \"links\": { \"first\": \"...\", \"last\": \"...\", \"prev\": null, \"next\": \"...\" }\n}\n</code></pre> <p>Works identically for <code>CursorPaginator</code> and for a <code>ResourceCollection</code> built on top of either paginator.</p>"},{"location":"packages/laravel-api-response/responses/#resources","title":"Resources","text":"<pre><code>api()-&gt;resource(UserResource::make($user));\napi()-&gt;resourceCollection(UserResource::collection($users));\n</code></pre>"},{"location":"packages/laravel-api-response/responses/#next","title":"Next","text":"<p>Customization covers the envelope shape, global metadata, rate limiting, audit logging, locale resolution, hooks/events and exception handling.</p>"},{"location":"packages/laravel-api-response/upgrade/","title":"Upgrade Guide","text":""},{"location":"packages/laravel-api-response/upgrade/#backward-compatibility-migration","title":"Backward compatibility &amp; migration","text":"<p>The pre-2.0 API (<code>BResponseSuccess</code>, <code>BResponseError</code>, <code>BResponseValidationError</code>, <code>BResponseNotFound</code>, <code>BResponseInternalError</code> and the underlying <code>Helpers\\BaseResponse</code>) still works unchanged and is not going away - it is marked <code>@deprecated</code> in favor of <code>api()</code>/<code>ApiResponse</code>, nothing more.</p> <p>One behavioral fix ships alongside: previously <code>toJson()</code> duplicated (and diverged from) <code>toArray()</code>'s logic, ignoring <code>config('keys')</code>/<code>config('wrap')</code> entirely and emitting the raw HTTP status code as the <code>status</code> value. <code>toJson()</code> now simply delegates to <code>toArray()</code>, so both honor your configured keys/wrap, and <code>status</code> is a boolean (<code>&lt; 400</code>) as documented - matching the shape every new response produced by <code>api()</code> already used.</p> <pre><code>// still works exactly as before\nuse Bhry98\\LaravelApiResponse\\Facades\\BResponseSuccess;\n\nreturn BResponseSuccess::make()\n    -&gt;message('Everything is fine!')\n    -&gt;data(['id' =&gt; 1])\n    -&gt;additions(['request_id' =&gt; uniqid()])\n    -&gt;toJson();\n</code></pre>"},{"location":"packages/laravel-api-response/upgrade/#new-in-this-release","title":"New in this release","text":"<p>Three cross-cutting concerns joined the package as opt-in middleware, each config-driven with sensible defaults - nothing below changes behavior until you turn it on:</p> <ul> <li>Rate limiting (<code>api.rate_limit</code>) - see Customization \u2192 Rate limiting.</li> <li>Audit logging (<code>api.audit</code>) - see Customization \u2192 Audit logging. Requires publishing and running the new migration if you enable it.</li> <li>Locale resolution (<code>api.locale</code>) - see Customization \u2192 Localization.</li> </ul> <p>If you're upgrading from a version before these existed, re-publish the config to pick up the new <code>rate_limit</code>, <code>audit_log</code> and <code>locale</code> sections:</p> <pre><code>php artisan vendor:publish --tag=bhry98-api-response-config --force\n</code></pre> <p>(Back up any custom edits to <code>config/bhry98-api-response.php</code> first - <code>--force</code> overwrites the whole file.)</p>"},{"location":"packages/laravel-api-response/upgrade/#architecture","title":"Architecture","text":"<pre><code>src/\n  Builder/        ResponseBuilder + its Concerns traits (fluent state, HTTP/business shortcuts, templates, terminal methods)\n  Contracts/      Interfaces: ResponseBuilderContract, ResponseFormatterContract, PresetContract, DataResolverContract, MetaProviderContract, PipelineStageContract\n  Data/           ResponsePayload (the mutable value object threaded through the pipeline)\n  Enums/          MetaKey, Preset\n  Events/         ResponseCreated, ResponseFailed, ResponseSent\n  Exceptions/     TemplateNotFoundException, PresetNotFoundException, ExceptionRenderer\n  Facades/        ApiResponse (+ deprecated legacy facades)\n  Formatters/     DefaultFormatter, MinimalFormatter, MobileFormatter, JsonApiFormatter\n  Helpers/        Legacy BaseResponse / ResponseKeys (deprecated, BC only)\n  Hooks/          HookRegistry (before/after)\n  Jobs/           StoreAuditLogEntry (queued audit log writer)\n  Middleware/     AttachRequestId, ApiRateLimit, AuditRequests, ResolveLocale (all optional)\n  Models/         AuditLog\n  Pipeline/       ResponsePipeline + Stages (data resolution, global meta, debug, hooks, events)\n  Presets/        PresetManager + built-in presets\n  Providers/      BResponseServiceProvider\n  Responses/      Legacy per-type response classes (deprecated, BC only)\n  Support/        DataResolver, MetaProviderRegistry, DebugCollector, meta providers, response-factory override\n  Testing/        TestResponse assertion macros\ndatabase/\n  migrations/     Publishable audit_log table migration\n</code></pre> <p>Every piece is bound through the container and swappable - nothing is a static god-class, and <code>ResponseBuilder</code> itself is always resolved fresh (never a singleton) so fluent state is never shared between responses.</p>"},{"location":"portfolio/","title":"\ud83d\udc4b Hi, I'm BHR Abdelrahman","text":"<p>Welcome to my personal documentation and portfolio.</p> <p>I'm a Software &amp; Infrastructure Engineer passionate about building scalable applications, cloud infrastructure, automation, and open-source Laravel packages.</p>"},{"location":"portfolio/#open-source","title":"\u2764\ufe0f Open Source","text":"<p>I enjoy building tools that help developers write better software faster.</p> <p>Contributions, feedback, and feature requests are always welcome.</p>"},{"location":"portfolio/#contact","title":"\ud83d\udcec Contact","text":"<p>Feel free to reach out.</p> <ul> <li>GitHub: https://github.com/bhry98</li> <li>LinkedIn: (Coming Soon)</li> <li>Email: (Coming Soon)</li> </ul>"},{"location":"portfolio/#support","title":"\u2b50 Support","text":"<p>If you find one of my projects useful:</p> <ul> <li>\u2b50 Star the repository</li> <li>\ud83d\udc1b Report issues</li> <li>\ud83d\udca1 Suggest new features</li> <li>\ud83e\udd1d Contribute via pull requests</li> </ul> <p>\"Build once. Improve forever.\"</p>"},{"location":"portfolio/about-me/","title":"\ud83d\ude80 About Me","text":"<ul> <li>\ud83d\udcbb Software Engineer</li> <li>\u2601\ufe0f Infrastructure &amp; DevOps Engineer</li> <li>\ud83d\udc18 Laravel &amp; PHP Developer</li> <li>\u2699\ufe0f Linux &amp; Red Hat Enthusiast</li> <li>\ud83d\udd10 Identity &amp; Access Management (Keycloak)</li> <li>\ud83d\udea2 Docker, Kubernetes &amp; OpenShift</li> <li>\ud83e\udd16 AI &amp; Automation Explorer</li> <li>\ud83c\udf0d Open Source Contributor</li> </ul>"},{"location":"portfolio/packages/","title":"Packages","text":""},{"location":"portfolio/packages/#open-source-packages","title":"\ud83d\udce6 Open Source Packages","text":"<p>I build reusable Laravel packages focused on developer productivity.</p>"},{"location":"portfolio/packages/#laravel-api-response","title":"Laravel API Response","text":"<p>A powerful response builder for Laravel APIs.</p> <ul> <li>Standardized JSON responses</li> <li>Ready-made success &amp; error responses</li> <li>Custom response macros</li> <li>Configurable behavior</li> </ul>"},{"location":"portfolio/packages/#laravel-users-core","title":"Laravel Users Core","text":"<p>A complete users management foundation for Laravel.</p> <p>Features include:</p> <ul> <li>User management</li> <li>Roles &amp; Permissions</li> <li>Authentication</li> <li>Profiles</li> <li>APIs</li> </ul>"},{"location":"portfolio/packages/#laravel-ready","title":"Laravel Ready","text":"<p>Starter package containing reusable Laravel components.</p>"},{"location":"portfolio/projects/","title":"\ud83d\ude80 Projects","text":"<p>Some of the projects I work on include:</p> <ul> <li>Enterprise ERP System</li> <li>Service Desk Platform</li> <li>HR Management System</li> <li>CRM</li> <li>Self-Service Portal</li> <li>AI Dashboard</li> <li>Knowledge Base Platform</li> </ul>"},{"location":"portfolio/skills/","title":"\ud83d\udee0\ufe0f Skills","text":""},{"location":"portfolio/skills/#backend","title":"Backend","text":"<ul> <li>Laravel</li> <li>PHP</li> <li>Java (Spring Boot)</li> <li>REST APIs</li> <li>Authentication &amp; Authorization</li> </ul>"},{"location":"portfolio/skills/#frontend","title":"Frontend","text":"<ul> <li>Livewire</li> <li>Filament</li> <li>Blade</li> <li>JavaScript</li> <li>HTML</li> <li>CSS</li> </ul>"},{"location":"portfolio/skills/#devops","title":"DevOps","text":"<ul> <li>Docker</li> <li>Kubernetes</li> <li>OpenShift</li> <li>GitHub Actions</li> <li>GitLab CI/CD</li> <li>Nginx</li> </ul>"},{"location":"portfolio/skills/#infrastructure","title":"Infrastructure","text":"<ul> <li>Red Hat Enterprise Linux</li> <li>Ubuntu</li> <li>Windows Server</li> <li>Active Directory</li> <li>DNS</li> <li>DHCP</li> <li>Virtualization</li> </ul>"},{"location":"portfolio/skills/#cloud-storage","title":"Cloud &amp; Storage","text":"<ul> <li>MinIO</li> <li>S3 Storage</li> <li>Object Storage</li> <li>NAS</li> </ul>"},{"location":"portfolio/skills/#security","title":"Security","text":"<ul> <li>Keycloak</li> <li>OAuth2</li> <li>OpenID Connect</li> <li>LDAP</li> <li>PAM Solutions</li> </ul>"}]}